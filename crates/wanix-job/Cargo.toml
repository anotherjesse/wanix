[package]
name = "wanix-job"
description = "Job-protocol vocabulary for ADR 0009: lifecycle states, the shared error taxonomy, status/result report shapes, and the wanix.resource spec envelope."
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
version.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
serde_json = "1"

[lib]
name = "wanix_job"
path = "src/lib.rs"
