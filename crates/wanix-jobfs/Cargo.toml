[package]
name = "wanix-jobfs"
description = "Job-protocol device machinery (ADR 0009): the shared job table, lifecycle/quota core, runner seam, and job-directory File implementations behind ToolFS and later adopters."
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1"
wanix-fs = { path = "../wanix-fs" }
wanix-job = { path = "../wanix-job" }

[lib]
name = "wanix_jobfs"
path = "src/lib.rs"

[lints]
workspace = true
