[package]
name = "wanix-agent"
description = "Agent device filesystem fronting an LLM session (codex app-server) as Wanix files."
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
wanix-fs = { path = "../wanix-fs" }
serde_json = "1"
base64 = "0.22"

[lib]
name = "wanix_agent"
path = "src/lib.rs"

[lints]
workspace = true
